import React from 'react';
import { Button } from '@material-ui/core';

const BtnAgregarArea = () => {
    return (
        <Button
            variant="contained"
            color="primary"
            size="large"
        >
            agregar area
        </Button>
    )
}

export default BtnAgregarArea;
